branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
  echo ""
  echo "⛔ main branch'e direkt push YASAK!"
  echo ""
  echo "Doğru akış:"
  echo "  1. git checkout dev"
  echo "  2. Geliştirme yap + test et"
  echo "  3. git checkout main && git merge dev"
  echo "  4. git push origin main"
  echo ""
  exit 1
fi
